import { useState } from 'react';
import { useLocale } from '../app/locale';
import { resolveCard } from '../app/presentation';
import { CombatantFigure } from '../components/CombatantFigure';
import { DIFFICULTIES } from '../data/ascension/modifiers';
import { CHARACTERS } from '../data/characters/characters';
import { getCustomContentPack } from '../game/content/customContent';
import type { GameState } from '../game/engine/types';

interface CharacterSelectPageProps { state: GameState; onStart: (characterId: string, difficulty: number) => void; onBack: () => void; }
export function CharacterSelectPage({ state, onStart, onBack }: CharacterSelectPageProps) {
 const { t } = useLocale(); const unlocked = state.metaProgress.unlockedCharacterIds; const maxDifficulty = state.metaProgress.maxUnlockedAscensionLevel; const customCharacterIds = new Set(Object.keys(getCustomContentPack().characters)); const [selectedId, setSelectedId] = useState(unlocked[0] ?? Object.keys(CHARACTERS)[0] ?? 'wanderer'); const [difficulty, setDifficulty] = useState(Math.min(state.metaProgress.ascensionLevel, maxDifficulty)); const selected = CHARACTERS[selectedId] ?? CHARACTERS.wanderer;
 return <main className="run-shell character-shell"><header className="run-header"><div><p className="eyebrow">CHOOSE THE VESSEL</p><h1>{t('character.title')}</h1><p className="subtitle">{t('character.subtitle')}</p></div><button className="text-button" onClick={onBack}>{t('back')}</button></header><section className="character-grid" aria-label={t('character.list')}>{Object.values(CHARACTERS).map((character) => { const isUnlocked = unlocked.includes(character.id) || customCharacterIds.has(character.id); const isSelected = selected.id === character.id; return <button key={character.id} className={`character-option ${isSelected ? 'selected' : ''}`} disabled={!isUnlocked} aria-pressed={isSelected} onClick={() => setSelectedId(character.id)}><span className={`character-mark character-${character.id}`} aria-hidden="true">{character.name.slice(0, 1)}</span><span><strong>{character.name}</strong><small>{isUnlocked ? `${character.signature ?? t('character.contract')} · ${t('character.hp')} ${character.maxHp}` : t('character.locked')}</small></span></button>; })}</section><section className="difficulty-section panel" aria-labelledby="difficulty-title"><div className="section-heading"><div><p className="eyebrow">ASCENSION TRIAL</p><h2 id="difficulty-title">{t('character.difficulty')}</h2></div><span className="difficulty-progress">{t('character.open', { level: maxDifficulty })}</span></div><div className="difficulty-grid">{DIFFICULTIES.map((entry) => { const locked = entry.level > maxDifficulty; const selectedDifficulty = entry.level === difficulty; return <button key={entry.level} className={`difficulty-card ${selectedDifficulty ? 'selected' : ''}`} disabled={locked} aria-pressed={selectedDifficulty} onClick={() => setDifficulty(entry.level)}><span className="difficulty-number">{entry.level}</span><span><strong>{entry.name}</strong><small>{locked ? entry.level === 4 ? t('character.unlock3') : t('character.unlock4') : entry.description}</small><em>{locked ? t('character.sealed') : t('character.region', { regions: entry.mapUnlocks.join('、') })}</em></span></button>; })}</div></section><section className="panel character-detail"><div className="character-portrait" aria-hidden="true"><CombatantFigure id={selected.id} label={selected.name} side="player" /></div><div className="character-detail-copy"><p className="eyebrow">{t('character.selected', { level: difficulty })}</p><h2>{selected.name}</h2><p className="character-description">{selected.description}</p><div className="character-facts"><p><strong>{selected.maxHp}</strong><span>{t('character.startHp')}</span></p><p><strong>{selected.baseEnergy ?? 3}</strong><span>{t('character.energy')}</span></p><p><strong>{selected.startingDeck.length}</strong><span>{t('character.deck')}</span></p><p><strong>{selected.startingRelics.length}</strong><span>{t('character.relic')}</span></p></div><div className="starter-deck-preview"><span>{t('character.cards')}</span><div>{selected.startingDeck.map((id, index) => { const card = resolveCard(id); return <span className="starter-card-chip" key={`${id}-${index}`}><b className="starter-card-cost" aria-label={t('character.cost', { cost: card?.cost ?? 0 })}>{card?.cost ?? 0}</b>{card?.name ?? id}</span>; })}</div></div><button className="primary-button" onClick={() => onStart(selected.id, difficulty)}>{t('character.start', { name: selected.name })}</button></div></section></main>;
}
