import { useState } from 'react';
import { resolveCard } from '../app/presentation';
import { CombatantFigure } from '../components/CombatantFigure';
import { DIFFICULTIES } from '../data/ascension/modifiers';
import { CHARACTERS } from '../data/characters/characters';
import { getCustomContentPack } from '../game/content/customContent';
import type { GameState } from '../game/engine/types';

interface CharacterSelectPageProps { state: GameState; onStart: (characterId: string, difficulty: number) => void; onBack: () => void; }
export function CharacterSelectPage({ state, onStart, onBack }: CharacterSelectPageProps) {
 const unlocked = state.metaProgress.unlockedCharacterIds; const maxDifficulty = state.metaProgress.maxUnlockedAscensionLevel; const customCharacterIds = new Set(Object.keys(getCustomContentPack().characters)); const [selectedId, setSelectedId] = useState(unlocked[0] ?? Object.keys(CHARACTERS)[0] ?? 'wanderer'); const [difficulty, setDifficulty] = useState(Math.min(state.metaProgress.ascensionLevel, maxDifficulty)); const selected = CHARACTERS[selectedId] ?? CHARACTERS.wanderer;
 return <main className="run-shell character-shell"><header className="run-header"><div><p className="eyebrow">CHOOSE THE VESSEL</p><h1>选择角色与难度</h1><p className="subtitle">选择契约，再决定这次灰烬远征要承受怎样的试炼。</p></div><button className="text-button" onClick={onBack}>返回</button></header><section className="character-grid" aria-label="角色列表">{Object.values(CHARACTERS).map((character) => { const isUnlocked = unlocked.includes(character.id) || customCharacterIds.has(character.id); const isSelected = selected.id === character.id; return <button key={character.id} className={`character-option ${isSelected ? 'selected' : ''}`} disabled={!isUnlocked} aria-pressed={isSelected} onClick={() => setSelectedId(character.id)}><span className={`character-mark character-${character.id}`} aria-hidden="true">{character.name.slice(0, 1)}</span><span><strong>{character.name}</strong><small>{isUnlocked ? `${character.signature ?? '独特契约'} · 生命 ${character.maxHp}` : '此契约尚未解锁'}</small></span></button>; })}</section><section className="difficulty-section panel" aria-labelledby="difficulty-title"><div className="section-heading"><div><p className="eyebrow">ASCENSION TRIAL</p><h2 id="difficulty-title">远征难度</h2></div><span className="difficulty-progress">已开放 1–{maxDifficulty}</span></div><div className="difficulty-grid">{DIFFICULTIES.map((entry) => { const locked = entry.level > maxDifficulty; const selectedDifficulty = entry.level === difficulty; return <button key={entry.level} className={`difficulty-card ${selectedDifficulty ? 'selected' : ''}`} disabled={locked} aria-pressed={selectedDifficulty} onClick={() => setDifficulty(entry.level)}><span className="difficulty-number">{entry.level}</span><span><strong>{entry.name}</strong><small>{locked ? entry.level === 4 ? '通关难度 3 后解锁' : '通关难度 4 后解锁' : entry.description}</small><em>{locked ? '封印中' : `新地域：${entry.mapUnlocks.join('、')}`}</em></span></button>; })}</div></section><section className="panel character-detail"><div className="character-portrait" aria-hidden="true"><CombatantFigure id={selected.id} label={selected.name} side="player" /></div><div className="character-detail-copy"><p className="eyebrow">已选契约 · 难度 {difficulty}</p><h2>{selected.name}</h2><p className="character-description">{selected.description}</p><div className="character-facts"><p><strong>{selected.maxHp}</strong><span>初始生命</span></p><p><strong>{selected.baseEnergy ?? 3}</strong><span>初始能量</span></p><p><strong>{selected.startingDeck.length}</strong><span>起始牌组</span></p><p><strong>{selected.startingRelics.length}</strong><span>随身遗物</span></p></div><div className="starter-deck-preview"><span>起始卡牌</span><div>{selected.startingDeck.map((id, index) => { const card = resolveCard(id); return <span className="starter-card-chip" key={`${id}-${index}`}><b className="starter-card-cost" aria-label={`消耗 ${card?.cost ?? 0} 点能量`}>{card?.cost ?? 0}</b>{card?.name ?? id}</span>; })}</div></div><button className="primary-button" onClick={() => onStart(selected.id, difficulty)}>以 {selected.name} 开始 Run</button></div></section></main>;
}
